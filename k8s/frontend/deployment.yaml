apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: sqlinj
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: 510151297987.dkr.ecr.ap-southeast-2.amazonaws.com/sqlinj-frontend:f814ac7a93cef1e0619c456193c93c13d4cda863
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080  # nginx-unprivileged listens on 8080 (non-root)
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          securityContext:
            runAsNonRoot: true
            runAsUser: 101  # nginx user in nginxinc/nginx-unprivileged:alpine
            allowPrivilegeEscalation: false
