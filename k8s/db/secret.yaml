apiVersion: v1
kind: Secret
metadata:
  name: db-secret
  namespace: sqlinj
type: Opaque
# ──────────────────────────────────────────────────────────────────────────────
# TEMPLATE FILE — do not commit real credentials.
# Populate from a secrets manager or use the ExternalSecrets operator.
# For local testing, copy to secret.local.yaml (gitignored) and fill in values.
# ──────────────────────────────────────────────────────────────────────────────
stringData:
  POSTGRES_DB: sqlinjproject
  POSTGRES_USER: sql_lab_user
  POSTGRES_PASSWORD: "<CHANGE_ME>"
