version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 16
    commands:
      - cd frontend       # Navigate to frontend folder
      - npm install
  build:
    commands:
      - cd frontend
      - npm run build
artifacts:
  base-directory: frontend/build    # React build output
  files:
    - '**/*'
