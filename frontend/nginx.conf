server {
    listen 8080;  # non-privileged port — required for nginxinc/nginx-unprivileged

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # Required for React Router — serve index.html for all routes
        try_files $uri /index.html;
    }
}
