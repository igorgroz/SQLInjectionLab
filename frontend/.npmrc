# Resolve peer dependency conflicts during npm ci in CI/CD.
# tailwindcss@4 and react-scripts@5 have peer dep resolution that npm 10+
# rejects without this flag. Mirrors backend/.npmrc for consistency.
# TODO: remove when react-scripts is replaced with Vite or similar.
legacy-peer-deps=true
